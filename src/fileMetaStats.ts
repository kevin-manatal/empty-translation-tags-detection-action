// eslint-disable-next-line filenames/match-regex
export interface fileMetaStats {
  rootFolder: string
  numberOfLines: number
  numberOfFiles: number
}

export interface fileMeta {
  file: string
  numberOfLines: number
}
